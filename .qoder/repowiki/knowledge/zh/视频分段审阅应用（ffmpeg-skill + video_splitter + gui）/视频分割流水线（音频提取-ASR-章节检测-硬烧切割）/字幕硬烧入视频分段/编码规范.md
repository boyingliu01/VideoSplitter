- 对外暴露的公开 API 使用类型注解与完整 docstring 描述参数/返回值/异常，内部辅助函数以单下划线前缀标记私有。
- 对路径进行平台适配：Windows 下将反斜杠替换为正斜杠并对冒号转义后再传入 FFmpeg subtitles filter。
- 错误处理采用显式 raise：输入长度不一致抛 ValueError，FFmpeg 非零退出码抛 RuntimeError 并附带 stderr 片段。