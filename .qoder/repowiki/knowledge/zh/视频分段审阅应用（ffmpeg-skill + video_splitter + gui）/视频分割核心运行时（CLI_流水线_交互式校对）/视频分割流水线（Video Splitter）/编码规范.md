- 子模块通过构造函数接收统一的 `SplitConfig` 实例，禁止在子模块内部自行读取环境变量。
- 流水线中间状态以 JSON 文件（.transcript.json、.chapters.json）落地，支持 `resume` 模式断点续跑。
- 各阶段在 `Pipeline.run()` 中以 try/except 包裹并记录到 `steps_completed`，失败时抛出异常而非吞错。