- 子包间严格遵循 architecture.yaml 声明的单向依赖：ffmpeg-skill → video_splitter → gui，禁止反向或跨层导入
- 测试文件同时位于顶层 tests/ 与 video_splitter/tests/，统一使用 test_*.py / *_test.py / tests.py 命名并通过 pytest markers 标记 slow/integration