运行单元测试需确保 Python 环境已安装 PySide6，并在仓库根目录下执行 `pytest tests/`（`conftest.py` 会自动把根目录加入 `sys.path`）。运行 FFmpeg skill 评测需先安装 `ffmpeg` 并使其在 PATH 中可用，然后进入 `ffmpeg-video-workspace/` 执行 `python grade_all.py`，该脚本硬编码了 `WORKSPACE = r"E:\Private\skill开发\ffmpeg-video-workspace\iteration-1"`，需按实际路径修改。