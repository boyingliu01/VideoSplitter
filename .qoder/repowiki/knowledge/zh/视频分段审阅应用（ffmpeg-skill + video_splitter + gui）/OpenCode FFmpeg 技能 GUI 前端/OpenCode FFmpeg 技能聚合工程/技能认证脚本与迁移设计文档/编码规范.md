- 认证脚本统一通过 `sys.argv = ['skill-cert', ...]` 注入参数再 `from skill_cert.cli.main import main; main()` 启动，避免直接依赖 CLI 解析逻辑。
- 在导入主入口前设置 `os.environ['SKILL_CERT_*']` 环境变量来切换覆盖率阈值与 LLM Judge 开关，保持单入口多配置的可复用模式。